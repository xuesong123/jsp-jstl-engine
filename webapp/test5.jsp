<div>
<h1>${name}</h1>
<table border="${border}">
    <tr>
        <th>&#160;</th>
    </tr>
    <c:forEach var="cell" items="${data}">
        <tr>
            <th>${cell}</th>
        </tr>
    </c:forEach>
</table>

<table border="${border}">
    <tr>
        <th>&#160;</th>
    </tr>
    <c:forEach var="cell" items="${data}">
        <tr>
            <th>${cell}</th>
        </tr>
    </c:forEach>
</table>

<table border="${border}">
    <tr>
        <th>&#160;</th>
    </tr>
    <c:forEach var="cell" items="${data}">
        <tr>
            <th>${cell}</th>
        </tr>
    </c:forEach>
</table>

<table border="${border}">
    <tr>
        <th>&#160;</th>
    </tr>
    <c:forEach var="cell" items="${data}">
        <tr>
            <th>${cell}</th>
        </tr>
    </c:forEach>
</table>
</div>